# Agent Knowledge Radar - Python Dependencies
# Most scripts use only stdlib, but these are needed for specific features

# Repository ingestion (ingest_repo.py)
gitingest>=0.1.0

# Bridge server upgrade (knowledge_platform_bridge.py)
fastapi>=0.115.0
uvicorn>=0.34.0
pydantic>=2.0

# MCP Server (scripts/mcp_server.py)
mcp>=1.0.0

# Testing
pytest>=8.0
